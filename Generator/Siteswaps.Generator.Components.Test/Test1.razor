@using Siteswaps.Generator.Components.Internal
@inherits FluxorTestContext
@code
{
    [Test]
    public Task Verify_test()
    {
        var cut = Render(@<SiteswapOverview/>);
        return Verify(cut);
    }
    
    [Test]
    public Task Verify_smallerComponents()
    {
        var cut = Render(@<BaseInputs/>);
        return Verify(cut);
    }
}
